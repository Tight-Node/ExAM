export let getSomething = (thing) => {
    let somewhere = thing;
    return {
        somewhere
    };
};

export let getwAnotherThing = (stuff) => {
    let sum = 7 + stuff;
    return sum;
};

module.exports = Gunes;
